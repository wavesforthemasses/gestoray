{
	"name": "gestoray",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"dev:all": "concurrently --kill-others-on-fail -p \"[{name}]\" -c \"cyan.bold,yellow.bold,green.bold\" -n \"EMULATORS,SEED,VITE\" \"npm run emulators\" \"npx wait-on tcp:8080 tcp:9099 && npm run seed\" \"VITE_USE_EMULATORS=true npm run dev\"",
		"dev:emulators": "concurrently --kill-others-on-fail -p \"[{name}]\" -c \"cyan.bold,yellow.bold,green.bold\" -n \"EMULATORS,SEED,VITE\" \"npm run emulators\" \"npx wait-on tcp:8080 tcp:9099 && npm run seed\" \"VITE_USE_EMULATORS=true npm run dev\"",
		"seed": "node scripts/seed-emulators.js",
		"build": "vite build",
		"preview": "vite preview",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"emulators": "firebase emulators:start",
		"push": "git pull && git add . && git commit -m 'update' && git push -u origin main",
		"test:unit": "vitest run",
		"test:unit:watch": "vitest",
		"test:e2e": "playwright test",
		"generate": "node scripts/scaffold.js",
		"module:install": "node scripts/install-module.js",
		"module:uninstall": "node scripts/uninstall-module.js",
		"template:sync": "node scripts/sync-templates.js"
	},
	"devDependencies": {
		"@playwright/test": "^1.61.1",
		"@sveltejs/adapter-auto": "^7.0.1",
		"@sveltejs/kit": "^2.63.0",
		"@sveltejs/vite-plugin-svelte": "^7.1.2",
		"concurrently": "^10.0.3",
		"svelte": "^5.56.1",
		"svelte-check": "^4.6.0",
		"typescript": "^6.0.3",
		"vite": "^8.0.16",
		"vitest": "^4.1.10",
		"wait-on": "^9.1.0"
	},
	"dependencies": {
		"@lucide/svelte": "^1.21.0",
		"firebase": "^12.15.0",
		"firebase-admin": "^13.10.0",
		"lucide-svelte": "^1.0.1",
		"uuidv7": "^1.2.1"
	}
}
