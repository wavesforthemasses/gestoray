# 📦 Modulo Gestione Progetti (`projects`)

Modulo puro agnostico per la gestione dei progetti, contenitori e commesse aziendali.

## Installazione via CLI
```bash
npm run module:install -- --name projects
```

## Disinstallazione via CLI
```bash
npm run module:uninstall -- --name projects
```
